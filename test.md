
## 1. 详细需求说明书 (PRD)

### **功能清单 (Feature List)**
| 模块 | 功能点 | 详细描述 |
| :--- | :--- | :--- |
| **脚本管理** | 增删改查 | 支持上传 `.py` 脚本，在线编辑代码，删除不再需要的脚本。 |
| **执行策略** | 定时/即时 | 支持 **Cron 表达式**配置定时任务，支持点击按钮立即执行一次。 |
| **状态监控** | 实时看板 | 显示任务状态：`闲置`、`运行中`、`成功`、`失败`、`超时`。 |
| **日志追踪** | 实时输出 | 捕获脚本的 `stdout` 和 `stderr`，并在 Web 端通过滚动窗口实时展示。 |
| **资源隔离** | 独立进程 | 每个脚本在独立的子进程中运行，避免主程序崩溃导致任务全部失效。 |
| **持久化** | 配置存储 | 所有的任务配置、执行历史、Cron 规则必须存入 **SQLite**。 |

### **非功能需求**
* **自愈性**：如果管理后台重启，未完成的定时任务应自动重新加载。
* **低侵入**：不需要修改现有的 Python 脚本，直接通过路径调用即可。

---

## 2. 概要设计 (Architecture Design)

### **技术选型**
* **后端**: Python 3.10+ / FastAPI (异步高性能)
* **调度器**: APScheduler (BackgroundScheduler 模式)
* **数据库**: SQLAlchemy + SQLite (轻量、单文件)
* **前端**: Vue 3 + Tailwind CSS (快速构建 UI)

### **数据模型 (Schema)**
* **Task 表**: `id`, `name`, `script_path`, `cron_expr`, `status`, `last_run_time`, `is_active`
* **Log 表**: `id`, `task_id`, `start_time`, `end_time`, `output`, `exit_code`

---

## 3. 如何让它“不开发完不停止”？

要实现这个效果，你不能只发一段话，你需要利用 Claude Code 的**“指令遵循”**特性。请按照以下步骤操作：

### **第一步：建立“军令状” (CLAUDE.md)**
在当前目录下手动创建一个文件名为 `CLAUDE.md`，内容如下：

```markdown
# PyCron-Master 开发规约

## 核心目标
构建一个完整的 Python 脚本定时管理工具。

## 任务清单 (Todo List) - 未完成请勿停止
- [ ] 1. 初始化项目结构与虚拟环境配置
- [ ] 2. 编写 SQLAlchemy 模型与 SQLite 初始化逻辑
- [ ] 3. 实现 FastAPI 后端 API (CRUD 任务)
- [ ] 4. 集成 APScheduler 实现核心调度引擎
- [ ] 5. 实现子进程捕获脚本日志的逻辑
- [ ] 6. 开发 Vue3 前端任务管理列表页
- [ ] 7. 开发前端实时日志查看器 (WebSocket 或 长轮询)
- [ ] 8. 联调并进行完整的任务运行测试

## 行为准则
1. **自主循环**：每完成清单中的一项，请自动勾选 `[x]` 并直接开始下一项，无需询问。
2. **错误处理**：遇到报错请自行修复，直到测试通过。
3. **完成标准**：直到所有清单项目全部勾选完成，并演示项目可运行后，方可停止。
```

### **第二步：下达启动指令**
在 Claude Code 终端输入：

> “我已经配置好了 `CLAUDE.md`。请读取其中的任务清单，**开启自主编程模式**。请严格按照清单顺序开发，**在完成所有功能并确保项目能跑起来之前，不要停止你的循环**。现在开始执行第 1 项：初始化项目结构。”

---

### **为什么这样能行？**
1.  **明确的边界**：有了勾选框 `[ ]`，AI 有了视觉上的“完工进度感”。
2.  **减少交互**：通过在 `CLAUDE.md` 里写明“无需询问”，它会减少停下来等确认的频率。
3.  **上下文持久化**：即便中间因为 Token 限制或网络闪断停止了，你再次输入 `continue`，它读取 `CLAUDE.md` 就能立刻知道自己干到哪了。

